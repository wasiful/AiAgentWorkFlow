# Ai Agent Work Flow

Run the ps1 codes from terminal opened in that directory it will help to install, configure, update and maintain tour Hermes Agent in your desktop environment utilizing UV
Use commands in power shell like this

powershell -ExecutionPolicy Bypass -File .\install_hermes.ps1

Takes some time for hermes to build everything and all the packages to install.

To use the Ui you should have a python env in this directory as well and necessery packages installed.

If you cant find the directories then search for them using your default device name, user profile as it is different for different person.

env path is the main thing in this method of installation.

once all the installations are done
Use the python Ui to run Hermese any time painlessly by clicking a button.

Configure Hermes according to NousResearch descriptions using subscription plans or ollama locally or both

Pushing the necessery codes for running and improving ai agents for your basic needs
